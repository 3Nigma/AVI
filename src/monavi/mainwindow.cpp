#include "mainwindow.hpp"

MainWindow::MainWindow() {
  // load the UI
  //Glib::RefPtr<Gtk::Builder> appBuilder = Gtk::Builder::create_from_file("res.glade");
  //Gtk::Window *mainWindow = this;
  //appBuilder->get_widget("MainWindow", mainWindow);
  //if(nullptr != settFrm)
  //  settFrm->run();
}
